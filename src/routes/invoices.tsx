import { Layout } from 'src/components/Layout';

export const Invoices = () => {
  return (
    <Layout>
      <main style={{ padding: '1rem 0' }}>
        <h2>Invoices</h2>
      </main>
    </Layout>
  );
};
